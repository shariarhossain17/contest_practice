#include <bits/stdc++.h>
using namespace std;

void solve(int tlight)
{
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;

    int cnt = 0;
    while (t--)
    {
        cnt++;
        solve(cnt);
    }
    return 0;
}